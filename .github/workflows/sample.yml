name: Our first pipeline!

on: [push]

jobs:
  says-hi:
    runs-on: ubuntu-latest
    steps:
      - name: Echo
        run: echo "hi"
      - name: Check Python version
        run: python --version
      - name: Reveal the password!
        run: echo {{$SECRETS.PASSWORD}}